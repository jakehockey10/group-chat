<p id="notice"><%= notice %></p>

<h1>Chatrooms</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @chatrooms.each do |chatroom| %>
      <tr>
        <td class="lead"><%= chatroom.name %></td>
        <!--<td><%# link_to 'Edit', edit_chatroom_path(chatroom) %></td>-->
        <!--<td><%# link_to 'Destroy', chatroom, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
        <td>
          <%= link_to fa_icon('sign-in', text: 'Join'),
                      chatroom_chatroom_users_path(chatroom),
                      method: :post,
                      class: 'btn btn-success' %>
        </td>
        <td>
          <%= link_to fa_icon('sign-out', text: 'Leave'),
                      chatroom_chatroom_users_path(chatroom),
                      method: :delete,
                      class: 'btn btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Chatroom', new_chatroom_path %>
